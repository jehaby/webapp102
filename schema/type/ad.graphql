type AdsEdge {
    cursor: ID!
    node: Ad
}

type AdConnection {
    totalCount: Int!
    edges: [AdsEdge]
    pageInfo: PageInfo!
}

type Ad {
    uuid: ID!
    name: String!
    description: String!
    component: Component!
    user: User
    price: Int!
    currency: Currency!
    createdAt: Time!
    updatedAt: Time
}
