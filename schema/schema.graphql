schema {
    query: Query
#    mutation: Mutation
}
type Query {
    categories(): [Category!]!

    manufacturers(): [Manufacturer!]!

    component(id: ID!): Component!
    # components(name: String): Component

    ad(uuid: ID!): Ad!
    ads(args: AdsArgs): AdConnection!
}

# type Mutation {
#     createComponent()
#     editComponent()
#     removeComponent()

#     createAd(email: String!, password: String!): Ad
#     editAd()
#     removeAd()
# }

input AdsArgs {
    first: Int
    # after: String
    # # categories: [Category]
    # components: [Component]
    # price: MinMax
    # weight: MinMax
    # location: String
}

type MinMax {
    Min: Int
    Max: Int
}
